#!/bin/bash
# Run this script once to install the necessary prerequisites for building the bootloader
# This script is intended to be run on a fresh Ubuntu 24.04 installation
sudo apt-get install -y bison flex libssl-dev libgnutls28-dev gcc-arm-linux-gnueabihf